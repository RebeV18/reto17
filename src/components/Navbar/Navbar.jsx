import { Link } from "react-router-dom"
import "./Navbar.css"

export const Navbar = () => {
    return (
        <nav className="nav-container">
            <h1>Lista de tareas</h1>
        </nav>
    )
}